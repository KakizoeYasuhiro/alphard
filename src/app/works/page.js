import WorksClient from '@/components/pages/WorksClient';

export const metadata = {
  title: 'Alphard - WORKS',
  description: 'Our services and work portfolio at Alphard',
};

export default function WorksPage() {
  return <WorksClient />;
}