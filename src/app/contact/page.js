import ContactClient from '@/components/pages/ContactClient';

export const metadata = {
  title: 'Alphard - CONTACT',
  description: 'Contact Alphard for any inquiries',
};

export default function ContactPage() {
  return <ContactClient />;
}